<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Day divider label -->
    <TextView
        android:id="@+id/tvDayDivider"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Today"
        android:textSize="10sp"
        android:textColor="@color/textHint"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:background="@drawable/bg_badge"
        android:layout_marginBottom="8dp"
        android:visibility="gone"/>

    <!-- ═══ RECEIVED bubble (left-aligned row with avatar) ═══ -->
    <LinearLayout
        android:id="@+id/bubbleReceived"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:layout_marginTop="2dp"
        android:visibility="gone">

        <!-- Sender avatar (circle initials) -->
        <TextView
            android:id="@+id/tvReceivedAvatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:gravity="center"
            android:text="AB"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:background="@drawable/bg_avatar"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="2dp"/>

        <!-- Bubble + meta column -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Text bubble -->
            <TextView
                android:id="@+id/tvReceivedContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="240dp"
                android:text="Message content here"
                android:textSize="13sp"
                android:textColor="@color/textPrimary"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:background="@drawable/bg_card"
                android:lineSpacingMultiplier="1.2"
                android:visibility="gone"/>

            <!-- Attachment preview (received) -->
            <LinearLayout
                android:id="@+id/layoutReceivedAttachment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="@drawable/bg_card"
                android:padding="10dp"
                android:layout_marginTop="2dp"
                android:visibility="gone">
                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_attachment"
                    android:tint="@color/colorPrimary"
                    android:layout_marginEnd="8dp"/>
                <TextView
                    android:id="@+id/tvReceivedFileName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="attachment.pdf"
                    android:textSize="12sp"
                    android:textColor="@color/colorPrimary"
                    android:maxWidth="160dp"
                    android:singleLine="true"
                    android:ellipsize="end"/>
            </LinearLayout>

            <!-- Time -->
            <TextView
                android:id="@+id/tvReceivedTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="10:30"
                android:textSize="10sp"
                android:textColor="@color/textHint"
                android:layout_marginTop="2dp"/>
        </LinearLayout>
    </LinearLayout>

    <!-- ═══ SENT bubble (right-aligned row with avatar) ═══ -->
    <LinearLayout
        android:id="@+id/bubbleSent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:layout_marginTop="2dp"
        android:visibility="gone">

        <!-- Spacer to push bubble to the right -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1"/>

        <!-- Bubble + meta column -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end">

            <!-- Text bubble -->
            <TextView
                android:id="@+id/tvSentContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="240dp"
                android:text="Sent message content"
                android:textSize="13sp"
                android:textColor="@color/white"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:background="@drawable/bg_button_gradient"
                android:lineSpacingMultiplier="1.2"
                android:visibility="gone"/>

            <!-- Attachment preview (sent) -->
            <LinearLayout
                android:id="@+id/layoutSentAttachment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="@drawable/bg_button_gradient"
                android:padding="10dp"
                android:layout_marginTop="2dp"
                android:visibility="gone">
                <ImageView
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:src="@drawable/ic_attachment"
                    android:tint="@color/white"
                    android:layout_marginEnd="8dp"/>
                <TextView
                    android:id="@+id/tvSentFileName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="attachment.pdf"
                    android:textSize="12sp"
                    android:textColor="@color/white"
                    android:maxWidth="160dp"
                    android:singleLine="true"
                    android:ellipsize="end"/>
            </LinearLayout>

            <!-- Time + read status row -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_gravity="end"
                android:layout_marginTop="2dp">

                <TextView
                    android:id="@+id/tvSentEdited"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="(edited)"
                    android:textSize="10sp"
                    android:textColor="@color/textHint"
                    android:fontFamily="sans-serif-light"
                    android:layout_marginEnd="4dp"
                    android:visibility="gone"/>

                <TextView
                    android:id="@+id/tvSentTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="10:30"
                    android:textSize="10sp"
                    android:textColor="@color/textHint"
                    android:layout_marginEnd="4dp"/>

                <!-- Read status checkmarks -->
                <TextView
                    android:id="@+id/tvReadStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="✓"
                    android:textSize="11sp"
                    android:textColor="@color/textHint"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Own avatar (circle initials) -->
        <TextView
            android:id="@+id/tvSentAvatar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:gravity="center"
            android:text="ME"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:background="@drawable/bg_avatar"
            android:layout_marginStart="8dp"
            android:layout_marginBottom="2dp"/>
    </LinearLayout>

</LinearLayout>
